{
  "name": "spotify-json-server",
  "version": "1.0.0",
  "scripts": {
    "generate-db": "node scripts/prepare-spotify-db.mjs data/spotify_songs.csv db.json",
    "start": "json-server --watch db.json --port 3000",
    "start:prod": "json-server --host 0.0.0.0 --port $PORT db.json"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
